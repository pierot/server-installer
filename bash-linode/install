#!/usr/bin/env bash

# $ bash < <( curl http://xxx )

# START
sudo apt-get --yes --quiet update && apt-get --yes --quiet upgrade

# BASE
sudo apt-get --yes --quiet install build-essential bison openssl libreadline5 libreadline-dev curl 
sudo apt-get --yes --quiet git-core zlib1g zlib1g-dev libssl-dev vim libsqlite3-0 libsqlite3-dev sqlite3 
sudo apt-get --yes --quiet libcurl4-openssl-dev libreadline-dev libxml2-dev git-core subversion autoconf
